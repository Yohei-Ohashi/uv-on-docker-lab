# =============================================================================
# Docker + uv ハイブリッド環境設定
# =============================================================================

# Jupyter Lab Settings
# ⚠️ 重要: JUPYTER_TOKENは必ず設定してください（セキュリティ上必須）
# 生成方法: python -c "import secrets; print(secrets.token_hex(32))"
JUPYTER_TOKEN=your-secure-token-here
JUPYTER_PORT=8888
# パスワード認証を使う場合（トークンの代わり）
JUPYTER_PASSWORD=

# Development Settings
ENVIRONMENT=development
DEBUG=true
LOG_LEVEL=INFO

# Database Settings (例：将来の拡張用)
# DATABASE_URL=postgresql://user:password@localhost:5432/dbname
# REDIS_URL=redis://localhost:6379

# API Settings (例：将来の拡張用)
# API_KEY=your-api-key-here
# API_SECRET=your-api-secret-here

# File Paths
DATA_DIR=./data
OUTPUT_DIR=./output
NOTEBOOKS_DIR=./notebooks

# uv Settings
UV_CACHE_DIR=/root/.cache/uv
UV_COMPILE_BYTECODE=1
UV_LINK_MODE=copy

# Docker Settings
COMPOSE_PROJECT_NAME=uv-docker-project
COMPOSE_FILE=docker-compose.yml